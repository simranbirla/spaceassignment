import React from "react";
import Filters from "./Filters";

const DashBoard = (props) => {
  return (
    <div>
      <h1>Dashboard</h1>
      <Filters />
    </div>
  );
};

export default DashBoard;
